# Инвентаризация учётной записи и домена

Дата проверки: 20.07.2026

## Учётная запись

- Пользователь: `cj326260`
- Домашний каталог: `/home/c/cj326260`
- Shell: `/bin/bash`
- Основная группа по `id`: `customers`
- Владелец домашнего каталога: `cj326260:newcustomers`
- Права домашнего каталога: `0700`
- `umask`: `0077`

Вывод: учётная запись изолирована от других пользователей. Новые файлы по умолчанию создаются без доступа для группы и остальных пользователей.

## Ограничения процесса

- Максимум открытых файлов: `1024`
- Максимум процессов пользователя: `40`
- Размер стека: `8192 KB`
- Core dumps отключены: `0`
- Swap для сервера отсутствует по предыдущей проверке.

Ограничение `max user processes = 40` критично для будущей архитектуры прокси: нельзя проектировать решение с большим количеством отдельных процессов или воркеров.

## Дисковая квота

Команда `quota` отсутствует и недоступна без администратора. Индивидуальная квота аккаунта пока не установлена.

## Каталоги и существующие рабочие файлы

В домашнем каталоге обнаружены:

- `/home/c/cj326260/public_html`
- `/home/c/cj326260/mailrhby.ru`
- `/home/c/cj326260/mailrhby.ru/public_html`
- `/home/c/cj326260/envs`
- `/home/c/cj326260/logs`

Рабочие Python-файлы и watchdog-скрипты:

- `id_check_worker.py`
- `id_check_worker_watchdog.sh`
- `region_worker.py`
- `region_worker_watchdog.sh`
- `loop_test.py`

Эти файлы относятся к существующим рабочим задачам. Их содержимое, права и запуск не изменять.

## Действующие Python-процессы

Обнаружены три процесса пользователя, работающие с 12.07.2026:

- PID `24849`
- PID `266403`
- PID `286956`

Общие свойства:

- исполняемый файл: `/usr/bin/python3.10`
- рабочий каталог: `/home/c/cj326260`
- родительский PID: `1`
- состояние: `S`

Процессы считать критически важными. Не отправлять им сигналы, не перезапускать и не менять связанные файлы.

## Доступные среды выполнения

Доступны:

- Python: `/usr/bin/python`, `/usr/bin/python3`
- PHP: `/usr/bin/php`
- Perl: `/usr/local/bin/perl`
- curl: `/usr/bin/curl`
- wget: `/usr/bin/wget`
- git: `/usr/bin/git`

Node.js и npm не обнаружены.

## DNS домена `prx.mailrhby.ru`

Цепочка разрешения:

```text
prx.mailrhby.ru
  -> gykxozm5jw.cdn.twcstorage.ru
  -> gykxozm5jw.a.trbcdn.net
  -> 91.238.111.225
```

Домен обслуживается через CDN Timeweb.

## Ответ CDN

Проверка `https://prx.mailrhby.ru/` вернула:

- HTTP/2 `200`
- `server: nginx`
- `content-type: text/html; charset=utf-8`
- `content-length: 38873`
- `x-cdn-edge-cache: HIT`
- `x-cdn-edge-id: 2024`
- поддержка HTTP/3 через `alt-svc`

CDN отдал закэшированную копию страницы. Дата `Last-Modified`: `19.07.2026 04:44:35 GMT`.

## Предварительные выводы

1. Это ограниченная учётная запись виртуального хостинга, а не VPS с root-доступом.
2. Прокси потребуется реализовывать средствами, разрешёнными хостингом: вероятнее всего PHP, Python или правила веб-сервера через панель/`.htaccess`.
3. Из-за лимита в 40 процессов предпочтительно решение без постоянного набора многочисленных воркеров.
4. Перед проектированием необходимо точно установить корень домена, текущие файлы сайта, доступные обработчики и возможность обращения к origin в обход CDN.
5. Существующие Python-процессы и рабочие скрипты не затрагивать.
